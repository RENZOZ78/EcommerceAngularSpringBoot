import { SportManValidators } from './sport-man-validators';

describe('SportManValidators', () => {
  it('should create an instance', () => {
    expect(new SportManValidators()).toBeTruthy();
  });
});
